# Claude Chat Frontend

Node.js + Express frontend for the Claude Chat application.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```

## Running

Start the server:
```bash
node server.js
```

The UI will be available at http://localhost:3000.
